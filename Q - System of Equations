#include<iostream>
using namespace std;
int main()
{
    int n,m,a,b,x,y,count=0;
    cin>>n>>m;
    for(a=0;a<=n;a++){
        for(b=0;b<=m;b++){
            x=(a*a)+b;
            y=a+(b*b);
            if(x==n && y==m) count++;

        }
    }
    cout<<count<<endl;

}
