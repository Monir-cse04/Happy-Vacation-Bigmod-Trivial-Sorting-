#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    long long fact=1;
    cin>>n;
    for(int i=0;i<n;i++){
        fact*=(n-i);
        fact%=10000;
    }
    if(fact==0)cout<<"0000";
    else cout<<fact;
    
    return 0;
}
